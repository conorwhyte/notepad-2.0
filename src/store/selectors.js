export const getValue = (state) => {
    return state.value;
};

export const getEditorValue = (state) => {
    return state.editor;
};

export const getName = (state) => {
    return state.name;
};

export const getId = (state) => {
    return state.id;
};

export const shouldShowWarning = (state) => {
    return state.showWarning;
}
